5.1 Практичне завдання: Комплексна функціональна анкета
Завдання:
Розробіть програму, що інтегрує всі розглянуті конструкції:
Live-coding demo:
Проведіть demo у браузері:

Створіть функцію createSurvey() , яка:
Використовує prompt() для отримання даних: ім’я, вік, місто.
Перетворює вік за допомогою Number().
Перевіряє, чи користувач повнолітній.
Повертає об’єкт з отриманими даними.
Створіть функцію displaySurvey(surveyData) , яка:
Виводить отримані дані у консоль та через alert(), використовуючи шаблонні
рядки.
Викличте ці функції та продемонструйте роботу.
Змініть значення змінної та запустіть prompt().
Продемонструйте, як виникають помилки при неправильному оголошенні змінних.

5.2 Практичне завдання: Калькулятор конвертації температур
Завдання:
Розробіть програму, яка конвертує температуру між градусами Цельсія (°C) та Фаренгейта
person.sayHello(); // Привіт, мене звуть Олена
3. Каррінг:
Напишіть функцію add(a) яка повертає іншу функцію, що додає число b до a .
function add(a) {
return function(b) {
return a + b;
};
}
const addTen = add(10);
console.log(addTen(5)); // 15

Що має робити програма:
Запитати у користувача числове значення температури.
Запитати напрямок конвертації: "C to F" або "F to C" (через prompt).
Використати функції та замикання для реалізації конвертера.
Використати функцію каррінгу, яка приймає коефіцієнти для перетворення.
Вивести результат конвертації через alert() та в консоль.

Функція createConverter() використовує каррінг для створення спеціалізованої
функції конвертації з заданими коефіцієнтами.
Користувач вводить числове значення температури та напрямок конвертації.
За допомогою умовного оператора обирається відповідний конвертер.
Результат конвертації виводиться як через alert(), так і в консоль.